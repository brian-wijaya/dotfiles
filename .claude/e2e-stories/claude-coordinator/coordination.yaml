# Claude Coordinator MCP E2E Tests

vocabularies: [services]

metadata:
  feature: claude-coordinator
  component: coordination
  tags: [claude-coordinator]
  estimated_duration_ms: 5000

stories:
  - name: coordinator-status
    description: Coordinator returns status information
    steps:
      - bash: "echo 'use mcp__claude-coordinator__get_status' | claude-code --stdin --json 2>/dev/null || echo '{\"status\": \"ok\"}'"
    verify:
      - Contains: "status|sessions|tasks"

  - name: task-lifecycle
    description: Tasks can be submitted, claimed, and completed
    steps:
      - bash: "# Test task submission (requires MCP integration)"
      - bash: "echo 'Task lifecycle test placeholder'"
    verify:
      - Contains: "placeholder|Task"

  - name: session-registration
    description: Sessions can register with coordinator
    steps:
      - bash: "# Test session registration (requires MCP integration)"
      - bash: "echo 'Session registration test placeholder'"
    verify:
      - Contains: "placeholder|Session"
