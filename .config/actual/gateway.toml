# Actual Gateway — User Configuration

[server]
transport = "http"   # "http" (multi-session daemon) or "stdio" (legacy)
port = 8372
grpc_port = 8374
# session_max_idle_seconds = 86400  # 24 hours (default)
# session_disconnect_grace_seconds = 30

[models]
tier_clerk = "qwen2.5-coder:7b"
tier_analyst = "qwen2.5:32b-instruct-q4_K_M"

[code_indexing]
enabled = true
root = "/home/bw/actual"

[postgresql]
enabled = true
host = "localhost"
port = 5432
username = "actual"
password = "actual"
database = "worldview"
pool_size = 10

[llm]
provider = "claude-code"
claude_binary = "/home/bw/.local/bin/claude"
claude_model = "sonnet"
max_concurrent = 3

[ollama]
enabled = false

[display]
enabled = true
auto_create = true
panel_height = 40
