# Starship prompt - Tokyo Night style
# Docs: https://starship.rs/config/

add_newline = false
command_timeout = 200

format = """
$directory\
$git_branch\
$git_status\
$character"""

[character]
success_symbol = "[❯](bold cyan)"
error_symbol = "[❯](bold red)"

[directory]
style = "bold cyan"
truncation_length = 3
truncation_symbol = "…/"
read_only = " 🔒"

[git_branch]
style = "bold purple"
format = "[$symbol$branch]($style) "
symbol = " "

[git_status]
style = "bold yellow"
format = '([$all_status$ahead_behind]($style) )'
ahead = "⇡${count}"
behind = "⇣${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"
conflicted = "="
untracked = "?"
stashed = "$"
modified = "!"
staged = "+"
renamed = "»"
deleted = "✘"
